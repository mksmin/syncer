# Repository instructions

- Use `master` as the sole default working branch for this repository.
- Do not rename `master` to `main` or create a replacement default branch unless the user explicitly asks.
